============
Installation
============

At the command line::

    $ easy_install sr_automation

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv sr_automation
    $ pip install sr_automation